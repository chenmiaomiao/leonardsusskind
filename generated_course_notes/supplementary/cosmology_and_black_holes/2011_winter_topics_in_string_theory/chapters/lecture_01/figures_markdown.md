# Figure Notes

## Verified Source Frames

- `lecture_01_figure_02.png` at `00:38:22.500`: the board is divided into `\(\alpha\ll1\)` and `\(\alpha\gg1\)` regimes. The weak-coupling side shows a compact center with radiating structure; the strong-coupling side is elongated. Susskind occludes part of both sketches, so the accompanying diagram records only the visible compact-versus-proliferating contrast.
- `lecture_01_figure_03.png` at `01:10:25.000`: this replacement frame shows the completed two-cycle construction. The wrapped geometry is visible at left and the F- and D-string ribbons are visible at right. It directly supports the aspect-ratio exchange and is substantially clearer than the discarded `01:05:52.960` frame.
- `lecture_01_figure_04.png` at `01:12:25.000`: this replacement frame coincides with the circle-versus-helix question. The red compact circle and its cut-open representation are visible while Susskind stands to the side. It directly supports the endpoint-identification diagram.

## Mathematical Reading

- Figure 02 supports only the qualitative comparison `\(\alpha\ll1\)` versus `\(\alpha\gg1\)`; no hidden chalk is reconstructed as an equation.
- Figure 03 supports two M2-brane wrappings. The companion equations `\(T_{(i)}=2\pi R_iT_{\mathrm{M2}}\)` and `\(T_{(1)}/T_{(2)}=R_1/R_2\)` are standard editorial clarifications of the lecture's geometric argument.
- Figure 04 supports the identification `\(y\sim y+2\pi R\)`. A helix requires simultaneous displacement along a non-compact direction; a fixed compact fiber is a circle.

## Rejected Frame

- `lecture_01_figure_06.png` is not used. It contains no new symmetric-point drawing or equality; it merely shows the old cylinder board during the later discussion. The chapter uses a clearly labeled pedagogical TikZ comparison instead of implying that `\(R_1=R_2\)` was visible in this frame.

## Review Result

All three retained screenshots occur at the stated subtitle intervals and support the surrounding discussion. The TeX diagrams distinguish visible board content from standard mathematical completion, and no figure is used as evidence for material that it does not show.
